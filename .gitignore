node_modules/
/dist/
src-tauri/target/
.DS_Store
*.log
.env
.env.local
src-tauri/.sidecar-src/
src-tauri/binaries/grok-build-*
!src-tauri/binaries/.gitkeep
packaging/windows/launcher/target/
# Anchored to the repository root. Without the leading slash these match at any
# depth, which silently swallowed .github/release/ — the file was never
# committed and CI checked out a tree that did not contain it.
/release/
/output/
/tmp/
docs/*.md
docs/**/*.md
docs/scorecard-aws-waf-fixture.json
docs/scorecard-session-ee2a709d.json
.env

# Phase1 local tool binary (rebuild via go build)
tools/utls-chrome-dial/utls-chrome-dial.exe
tools/utls-chrome-dial/utls-chrome-dial

test-results/
playwright-report/
